# Uso sugerido:
# docker compose --env-file .env_produccion -f docker-compose_produccion.yaml up -d

services:
  web:
    build: .
    command: daphne -b 0.0.0.0 -p 8000 project.asgi:application
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    expose:
      - "8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - path: .env_produccion
        required: false
    environment:
      - DJANGO_SETTINGS_MODULE=project.settings
      - DB_HOST=db
      - DB_PORT=5432
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - social_media:/app/apps/media
    networks:
      centinela-net:
        aliases:
          - social-consensus-web

  db:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    expose:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_DB=${DB_NAME:-centinela_social}
      - POSTGRES_USER=${DB_USER:-centinela_social_app}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-change-me-strong-db-password}
    command: postgres -c shared_preload_libraries=vector
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-centinela_social_app} -d ${DB_NAME:-centinela_social}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      centinela-net:
        aliases:
          - social-postgres

  redis:
    image: redis:6
    restart: unless-stopped
    expose:
      - "6379"
    volumes:
      - ./redis.conf:/usr/local/etc/redis/redis.conf:ro
    command: redis-server /usr/local/etc/redis/redis.conf
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      centinela-net:
        aliases:
          - social-redis

  celery_worker:
    build: .
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    command: celery -A project worker -l info
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - path: .env_produccion
        required: false
    environment:
      - DJANGO_SETTINGS_MODULE=project.settings
      - DB_HOST=db
      - DB_PORT=5432
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - social_media:/app/apps/media
    networks:
      - centinela-net

  celery_beat:
    build: .
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    command: celery -A project beat -l info
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - path: .env_produccion
        required: false
    environment:
      - DJANGO_SETTINGS_MODULE=project.settings
      - DB_HOST=db
      - DB_PORT=5432
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - social_media:/app/apps/media
    networks:
      - centinela-net

volumes:
  postgres_data:
    name: centinela_social_postgres_data
  social_media:
    name: centinela_social_media

networks:
  centinela-net:
    external: true
